{"pageProps":{"data":{"title":"Git Basics"},"content":"<p>Initialize a repository:</p>\n<p><code>git init</code></p>\n<p>Clone repository:</p>\n<p><code>git clone reponame</code></p>\n<p>Pull changes from remote:</p>\n<p><code>git pull</code></p>\n<p>Add all files to staging:</p>\n<p><code>git add .</code></p>\n<p>Removes and stages the file removal:</p>\n<p><code>git rm filenme</code></p>\n<p>Inline commit:</p>\n<p><code>git commit -m \"message\"</code></p>\n<p>Push changes to remote:</p>\n<p><code>git push</code></p>\n<p>Revert the changes to head:</p>\n<p><code>git reset --hard HEAD</code></p>\n<p><code>git clean -f -d</code></p>\n<p>Unstage all changes:</p>\n<p><code>git reset HEAD -- .</code></p>\n<p>Overwrite local changes with the state of the last commit:</p>\n<p><code>git checkout -- file</code></p>\n<p>Compare working directory with staging area (no output if files are staged):</p>\n<p><code>git diff</code></p>\n<p>Compare staged changes with the last commit:</p>\n<p><code>git diff --staged</code></p>\n<p>Store changes not ready to commit on the stack:</p>\n<p><code>git stash</code></p>\n<p>Apply and drop the changes from stack:</p>\n<p><code>git stash pop</code></p>\n<p>Viewing commit history in one line:</p>\n<p><code>git log --oneline</code></p>\n<p>List shortnames of each remote with urls:</p>\n<p><code>git remote -v</code></p>\n<p>Set an alias:</p>\n<p><code>git config --global alias.unstage 'reset HEAD --'</code></p>\n<p>Change branch:</p>\n<p><code>git checkout branchname</code></p>\n<p>Create a new branch and switch to it:</p>\n<p><code>git checkout -b newbranch</code></p>\n<p>Show unmerged files during a conflict:</p>\n<p><code>git merge > git status</code></p>\n<p>Generate SSH-key:</p>\n<p><code>ssh-keygen -t ed25519 -C \"email@example.com\"</code></p>\n<p>Git Tutorial:</p>\n<p><code>man gittutorial</code></p>\n<h3>Useful links</h3>\n<p><a href=\"https://training.github.com/\">GitHub Training</a></p>\n<p><a href=\"https://docs.gitlab.com/ee/gitlab-basics/index.html\">GitLab Basics</a></p>\n<p><a href=\"https://docs.gitlab.com/ee/university/training/\">GitLab Training</a></p>\n<p><a href=\"https://github.com/github/gitignore\">A collection of .gitignore templates</a></p>\n<p><a href=\"https://learngitbranching.js.org/\">Learn Git Branching</a></p>\n"},"__N_SSG":true}